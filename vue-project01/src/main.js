import { createApp } from 'vue'
import App from './App.vue'
import Count from './Myapp1.vue'

import './assets/main.css'

// createApp(App).mount('#app')
createApp(Count).mount('#app')
createApp(App).mount('#show')
